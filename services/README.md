# Services

Infrastructure adapters for external communication.

- `api`: HTTP client, interceptors, endpoint modules.
- `socket`: realtime transport (websocket/socket.io).
- `rtc`: call signaling and WebRTC helpers.
